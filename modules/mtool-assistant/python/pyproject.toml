[project]
name = "mtool-assistant"
version = "0.1.0"
dependencies = [
    "certifi>=2025.8.3",
    "yt-dlp>=2025.8.11",
]

[build-system]
requires = ["uv_build>=0.8.10,<0.9.0"]
build-backend = "uv_build"
